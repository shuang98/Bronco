<div class="container">
	<div class="row">
		<div class="col-sm-3">
			<%= render 'sidebar' %>
		</div>
		<div class="col-sm-8">
			<h1><%= @course.title %></h1>
			<div id="section-body">
				<% @sections.each do |section| %>
					<%= render partial: 'section_form', locals: {section: section} %>
				<% end %>
			</div>
		</div>
	</div>
