<span>Signed in as <%= current_user.username %> | </span> <%= link_to "logout", logout_path %>

<h1>Courses</h1>
<% @courses.each do |course| %>
	<div class="well">
		<%= course.title %>
	</div>
<% end %>

<%= link_to "Create Course", new_course_path, class: "btn btn-primary"%>



